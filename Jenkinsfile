pipeline {
    agent any
    stages{
        stage {
            steps{
                sh `echo "Hello World!"`
                sh '''
                    echo "Multiline shell steps works too"
                '''
            }
        }
    }
}